# خطوات الإنهاء والنشر

## أولا: GitHub Pages
ارفع محتويات هذا المجلد إلى المستودع الحالي. يجب أن يبقى `index.html` في جذر مصدر النشر.

## ثانيا: قاعدة البيانات
1. افتح Supabase وأنشئ مشروعاً جديداً.
2. افتح SQL Editor.
3. شغّل `database.sql`.
4. شغّل `seed.sql` إذا أردت الأدوية الأولية.
5. من Project Settings > API انسخ Project URL وPublishable/anon key.
6. ضع القيمتين في `app-config.js`.
7. لا تنسخ service_role key.

## ثالثا: الحسابات
أنشئ Auth للمستخدمين. عند تسجيل صيدلية:
- أنشئ حساب المستخدم.
- أنشئ سجل pharmacy مرتبطاً بـ owner_id.
- التسجيل يكون فورياً كما طلب صاحب المشروع.
- خزّن الموقع والرقم.
- لا تمنح المستخدم صلاحيات admin.

## رابعا: الصيدلية
بعد الدخول، يجب أن تستطيع:
- رؤية قائمة الأدوية.
- اختيار متوفر/غير متوفر.
- إدخال السعر.
- إدخال موعد متوقع للتوفر.
- رؤية وقت آخر تحديث.
- تعديل بياناتها وموقعها.

## خامسا: الإدارة
أنشئ حساب admin منفصلاً، وتكون له الصلاحيات من Backend/Custom Claims.
اللوحة تعرض:
- عدد الصيدليات.
- الأدوية الأكثر بحثاً.
- أكثر الأسئلة.
- طلبات التنبيه.
- التعليقات والردود.
- آخر تحديثات المخزون.

## سادسا: الإشعارات
طلبات التنبيه تسجل في `alert_requests`.
لإرسال إشعار حقيقي عند توفر الدواء، استخدم Edge Function/Backend متصل بخدمة إشعارات مناسبة. لا تضع مفتاح الخدمة في الواجهة.

## سابعا: الاختبار قبل الإعلان
اختبر:
- الهاتف Android.
- iPhone.
- البحث بأخطاء إملائية.
- الموقع.
- الاتصال.
- تسجيل صيدلية جديد.
- تحديث السعر.
- تحديث التوفر.
- طلب التنبيه.
- التعليق والرد.
- صلاحيات admin.
- محاولة مستخدم عادي تعديل مخزون صيدلية أخرى.

## النشر العام
بعد نجاح الاختبارات فقط، انشر رابط GitHub Pages للمستخدمين.
